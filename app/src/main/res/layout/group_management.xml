<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E0E0E0"
    android:orientation="vertical">

    <ListView
        android:id="@+id/listP"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/list_item_inner_padding"
        android:layout_marginRight="@dimen/list_item_inner_padding"
        android:divider="@android:color/transparent"
        android:dividerHeight="@dimen/list_item_inner_padding"
        android:scrollbars="none" >
    </ListView>

    <ImageView
        android:id="@+id/createButton"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:paddingLeft="@dimen/padding_icon"
        android:paddingRight="@dimen/padding_icon"
        android:paddingTop="@dimen/padding_icon"
        android:paddingBottom="@dimen/padding_icon"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:layout_marginRight="20dp"
        android:background="@drawable/button_create_schedule"
        android:src="@drawable/ic_add_schedule" />

</RelativeLayout>